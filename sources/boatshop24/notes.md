# Boatshop24 Notes

- Wave 2 marketplace benchmark source.
- Expected role if safely reachable: `broader_marketplace_backbone`.
- Country-bucket paths are kept tiny and public-facing only.
- Bounded Wave 2 result: public English-language listing and country-bucket pages returned `403` or access-denied behavior to the raw probe client.
- Final Wave 2 decision: `blocked_do_not_bypass`.
- Near-term recommendation: keep as a permission/feed/API candidate, not a raw public probe candidate.
